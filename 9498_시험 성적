/*
시험 점수를 입력받아 90 ~ 100점은 A, 80 ~ 89점은 B, 70 ~ 79점은 C, 
60 ~ 69점은 D, 나머지 점수는 F를 출력하는 프로그램을 작성하시오.
*/
#include <stdio.h>

int main(void) {

	int N;

	scanf("%d", &N);

	if(N>=90)
		puts("A");
	else if(N>=80)
		puts("B");
	else if(N>=70)
		puts("C");
	else if(N>=60)
		puts("D");
	else
		puts("F");

	return 0;
}
